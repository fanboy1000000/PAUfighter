{
  "name": "paufighter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "A browser-based parody UFC simulator: give fighters stats, seed the dice, watch the fight.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check:docs": "node tools/check-docs.mjs",
    "verify": "npm run typecheck && npm test && npm run build && npm run check:docs"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  }
}
